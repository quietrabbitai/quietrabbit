# Force devDependencies (typescript, vite, oxlint, @types/*) to install
# even though this machine's environment/global npm config sets
# omit=dev by default, which would otherwise silently drop them.
include=dev
